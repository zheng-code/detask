<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.zhengcode.detask">
    <!--android:hardwareAccelerated ="true"-->
    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">

        <activity
                android:name=".MainActivity"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <activity android:name=".DashboardActivity"></activity>
        <activity android:name=".TasksActivity"></activity>
        <activity android:name=".TaskManagerActivity"></activity>

        <activity android:name=".DashboardCurrentTasksActivity"></activity>
        <activity android:name=".DashboardEditProfileActivity"></activity>
        <activity android:name=".DashboardSkillsActivity"></activity>
        <activity android:name=".DashboardTraitsActivity"></activity>
        <activity android:name=".DashboardTaskHistoryActivity"></activity>
        <activity android:name=".DashboardSettingsActivity"></activity>
    </application>

</manifest>